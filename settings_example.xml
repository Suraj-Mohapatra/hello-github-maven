<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">

    <!-- Where Maven keeps your downloaded dependencies -->
    <!-- <localRepository>${user.home}/.m2/repository</localRepository> -->

    <mirrors>

        <!-- Default to Maven Central -->
        <mirror>
            <id>central</id>
            <mirrorOf>central</mirrorOf>
            <url>https://repo.maven.apache.org/maven2</url>
        </mirror>

        <!-- Mirror for all repositories except the Git Maven repo -->
        <!-- <mirror>
            <id>nexus</id>
            <mirrorOf>*,!git-maven</mirrorOf>
            <url>https://nexus.example.com:9443/nexus/content/groups/public</url>
        </mirror> -->

    </mirrors>

    <servers>

        <!-- GitHub Packages authentication -->
        <server>
            <id>github</id>
            <username>username</username>
            <password>token</password>
        </server>

        <!-- <server>
            <id>gitlab</id>
            <configuration>
                <httpHeaders>
                    <property>
                        <name>Token-Name</name>
                        <value>my-access-token</value>
                    </property>
                </httpHeaders>
            </configuration>
        </server> -->

    </servers>

    <profiles>
        <profile>
            <id>github</id>
            <repositories>

                <repository>
                    <id>central</id>
                    <url>https://repo.maven.apache.org/maven2</url>
                    <!-- <releases>
                        <enabled>true</enabled>
                        <updatePolicy>always</updatePolicy>
                    </releases>
                    <snapshots>
                        <enabled>true</enabled>
                        <updatePolicy>always</updatePolicy>
                    </snapshots> -->
                </repository>

                <repository>
                    <id>github</id>
                    <url>https://maven.pkg.github.com/my/repo</url>
                </repository>

                <!-- <repository>
                    <id>gitlab</id>
                    <url>https://my/gitab/url/repo</url>
                </repository> -->

            </repositories>

            <!-- <pluginRepositories>
                <pluginRepository>
                    <id>central</id>
                    <url>http://central.example.com</url>
                    <releases>
                        <enabled>true</enabled>
                        <updatePolicy>always</updatePolicy>
                    </releases>
                    <snapshots>
                        <enabled>true</enabled>
                        <updatePolicy>always</updatePolicy>
                    </snapshots>
                </pluginRepository>
            </pluginRepositories> -->
        </profile>
    </profiles>

    <activeProfiles>
        <activeProfile>github</activeProfile>
    </activeProfiles>

</settings>